; Loaded after www.conf / zz-docker.conf — last wins for these directives.
; Unix socket: no TCP clash with Railway $PORT (8080/9000/etc.) for nginx HTTP.
[www]
clear_env = no
listen = /var/run/php/php-fpm.sock
listen.owner = www-data
listen.group = www-data
listen.mode = 0660
